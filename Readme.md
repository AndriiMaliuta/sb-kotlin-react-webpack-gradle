# SB with React via Webpack

* Spring Boot
* Kotlin
* Gradle
* Webpack
* React

Use of Webpack to build NPM packages React app in a bundle and add to home.html for rendering.
As opposite to Maven Mojo Resources plugin, Gradle 
